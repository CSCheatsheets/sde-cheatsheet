# Language Identifier - Syntax

## Configuration Syntax

Use `conf` to identify configuration code blocks.

Syntax

````markdown
```conf
alias l="ls -lash"
```
````

Output

```conf
alias l="ls -lash"
```
